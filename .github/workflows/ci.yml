name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

defaults:
  run:
    working-directory: plugins/toolbar-controls

jobs:
  test-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - run: pnpm install --no-frozen-lockfile
      - run: pnpm test
      - run: pnpm build

      - name: Upload plugin artifacts
        uses: actions/upload-artifact@v4
        with:
          name: toolbar-controls
          path: |
            plugins/toolbar-controls/dist/magalz.toolbar-controls.mjs
            plugins/toolbar-controls/dist/magalz.toolbar-controls.json

  pages:
    if: github.ref == 'refs/heads/main'
    needs: test-build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    defaults:
      run:
        working-directory: plugins/toolbar-controls
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - run: pnpm install --no-frozen-lockfile
      - run: pnpm build

      - uses: actions/upload-pages-artifact@v3
        with:
          path: plugins/toolbar-controls/dist

      - id: deployment
        uses: actions/deploy-pages@v4
